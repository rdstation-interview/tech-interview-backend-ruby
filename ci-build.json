{
  "secret": "${WEBHOOK_SECRET}",
  "parameters": {
    "CONTAINER_IMAGE": "gcr.io/${GOOGLE_PROJECT_ID}/tech-interview-challenge:${CIRCLE_SHA1}",
    "BRANCH": "${CIRCLE_BRANCH}",
    "PR_NUMBER": "${PR_NUMBER}",
    "REPO_NAME": "${CIRCLE_PROJECT_REPONAME}",
    "SHORT_SHA": "${SHORT_GIT_HASH}",
    "AUTHOR": "${CIRCLE_USERNAME}"
  }
}

